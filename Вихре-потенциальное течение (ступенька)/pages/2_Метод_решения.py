import streamlit as st

st.set_page_config(page_title="Метод решения", layout="wide")

menu = st.sidebar.radio('***',
                        (
                         "Общая схема",
                         "Итерационный процесс",
                         "МКЭ: слабая форма",
                         "МКЭ: дискретизация",
                         "Алгоритм",
                         )
                        )


if menu == "Общая схема":
    st.markdown(r"""
    ##### Общая идея итерационного метода

    Схема процесса:

    Начальное предположение $\psi^0 \to$ Уточнение $\psi^1 \to$ Уточнение $\psi^2 \to \ldots \to$ Сходимость $\psi^*$

    На каждом шаге:
    1. По старому $\psi^k$ находим, где вихрь
    2. Считаем площадь вихревой зоны $S^k$
    3. Находим $\omega^k = \Gamma/S^k$
    4. Решаем уравнение $\to$ получаем новое $\psi^{k+1}$
    5. Проверяем, изменилось ли что-то. Если нет — стоп.
    """)

elif menu == "Итерационный процесс":
    st.markdown(r"""
    ##### Подробный разбор каждого шага

    **Шаг 1: Начальное приближение $\psi^0$**

    Решаем уравнение Лапласа — без вихря вообще:

    $$
    -\nabla^2 \psi^0 = 0
    $$

    Физический смысл: поток входит, плавно огибает уступ, выходит. Никакого вихря.

    **Шаг 2: Первая итерация ($k=0 \to k=1$)**

    *2.1. Находим вихревую зону.* В каждом треугольнике смотрим значение $\psi^0$ (в центре или в узлах). Правило: если $\psi^0 < 0$ в треугольнике — он вихревой.

    *2.2. Считаем площадь вихревой зоны:*

    $$
    S^0 = \sum_{\text{вихревые треугольники}} S_e
    $$

    *2.3. Находим завихрённость:*

    $$
    \omega^0 = \frac{\Gamma}{S^0}
    $$

    *2.4. Собираем правую часть уравнения.* Для каждого треугольника: вихревой: $f = \omega^0$, потенциальный: $f = 0$.

    *2.5. Решаем уравнение Пуассона:*

    $$
    -\nabla^2 \psi^1 = f
    $$

    Физически: в вихревой зоне появляется "источник" вращения, поток начинает закручиваться. Получаем $\psi^1$.

    **Шаг 3: Вторая итерация ($k=1 \to k=2$)**

    Смотрим на $\psi^1$. Возможно, вихрь стал больше или меньше. Новая площадь и новая завихрённость:

    $$
    S^1 = \sum_{\text{новые вихревые}} S_e, \qquad \omega^1 = \frac{\Gamma}{S^1}
    $$

    Важно: $S^1 \neq S^0$, поэтому $\omega^1 \neq \omega^0$. Решаем снова:

    $$
    -\nabla^2 \psi^2 = f(\omega^1)
    $$

    **Шаг 4: Проверка сходимости**

    Сравниваем $\psi^{k+1}$ и $\psi^k$:

    $$
    \text{error} = \frac{\sqrt{\int_\Omega (\psi^{k+1} - \psi^k)^2 \, d\mathbf{x}}}{\sqrt{\int_\Omega (\psi^{k+1})^2 \, d\mathbf{x}}}
    $$

    Если error < $10^{-6}$ — сошлось. Если нет — продолжаем итерации.
    """)

elif menu == "МКЭ: слабая форма":
    st.markdown(r"""
    ##### Метод конечных элементов

    Исходное уравнение:

    $$
    -\frac{\partial^2 \psi}{\partial x_1^2} - \frac{\partial^2 \psi}{\partial x_2^2} = f
    $$


    умножим на тестовую функцию $\phi$ и проинтегрируем:

    $$
    \int_\Omega \left(-\nabla^2 \psi\right) \phi \, d\mathbf{x} = \int_\Omega f \phi \, d\mathbf{x}
    $$

    Интегрируем левую часть по частям:

    $$
    \underbrace{\int_\Omega \nabla \psi \cdot \nabla \phi \, d\mathbf{x}}_{\text{новая левая часть}} - \underbrace{\int_{\partial\Omega} \frac{\partial \psi}{\partial n} \phi \, ds}_{\text{граничный интеграл}} = \int_\Omega f \phi \, d\mathbf{x}
    $$

    Граничный интеграл:
    - На стенках ($\Gamma_1, \Gamma_2$) и входе ($\Gamma_3$): $\phi = 0$ (по условию)
    - На выходе ($\Gamma_4$): $\frac{\partial \psi}{\partial n} = 0$ (по условию)

    **Итог:**

    $$
    \boxed{\int_\Omega \nabla \psi \cdot \nabla \phi \, d\mathbf{x} = \int_\Omega f \phi \, d\mathbf{x}}
    $$
    """)

elif menu == "МКЭ: дискретизация":
    st.markdown(r"""
    ##### Метод конечных элементов

    **Аппроксимация функции тока**

    Представляем $\psi$ как сумму базисных функций:

    $$
    \psi(x_1, x_2) \approx \sum_{j=1}^{N} \psi_j N_j(x_1, x_2)
    $$

    $N_j$ — функция, равная 1 в узле $j$ и 0 во всех остальных узлах. На каждом треугольнике $N_j$ — полином.

    **Подстановка в слабую форму**

    Берём $\phi = N_i$ (поочерёдно все базисные функции):

    $$
    \int_\Omega \nabla \left(\sum_{j=1}^{N} \psi_j N_j\right) \cdot \nabla N_i \, d\mathbf{x} = \int_\Omega f N_i \, d\mathbf{x}
    $$

    $$
    \sum_{j=1}^{N} \psi_j \underbrace{\int_\Omega \nabla N_j \cdot \nabla N_i \, d\mathbf{x}}_{K_{ij}} = \underbrace{\int_\Omega f N_i \, d\mathbf{x}}_{F_i}
    $$

    Получаем систему линейных уравнений:

    $$
    \sum_{j=1}^{N} K_{ij} \psi_j = F_i, \quad i = 1, \ldots, N
    $$

    Или в матричном виде: $\mathbf{K} \boldsymbol{\psi} = \mathbf{F}$.

    **Матрица $\mathbf{K}$**

    $$
    K_{ij} = \int_\Omega \nabla N_i \cdot \nabla N_j \, d\mathbf{x}
    $$

    Размер: $N \times N$, где $N$ — число узлов. Структура — разреженная.

    **Вычисление правой части $\mathbf{F}$**

    $$
    F_i = \int_\Omega f N_i \, d\mathbf{x} = \sum_{e=1}^{M} \int_{K_e} f N_i \, d\mathbf{x}
    $$
    """)

elif menu == "Алгоритм":
    st.markdown(r"""
    ##### Алгоритм

    Задать: $\Gamma$, $\varepsilon$, сетка, степень $p$.

    **ШАГ 0: Построить сетку (Gmsh)**
    - Треугольники, узлы, связи
    - Определить граничные узлы

    **ШАГ 1: Собрать матрицу $\mathbf{K}$**
    - Для каждого треугольника: вычислить $\mathbf{K}^{(e)}$ по формуле, добавить в глобальную $\mathbf{K}$

    **ШАГ 2: Начальное приближение $\psi^0$**
    - $f = 0$ везде
    - Собрать $\mathbf{F}^0$, учесть граничные условия
    - Решить $\mathbf{K} \psi^0 = \mathbf{F}^0$

    **НАЧАЛО ЦИКЛА** $k = 0, 1, 2, \ldots$

    **ШАГ 3a: Найти вихревую зону**
    - Для каждого треугольника: $\psi_{center} = \text{среднее}(\psi^k \text{ в узлах})$
    - Если $\psi_{center} < 0 \to$ вихревой

    **ШАГ 3b: Вычислить площадь $S^k$**
    - $S^k =$ сумма площадей вихревых треугольников

    **ШАГ 3c: Вычислить $\omega^k = \Gamma / S^k$**

    **ШАГ 3d: Собрать правую часть $\mathbf{F}^{k+1}$**
    - Для каждого треугольника: если вихревой: $f = \omega^k$, иначе: $f = 0$

    **ШАГ 3e: Учесть граничные условия**
    - Модификация $\mathbf{F}$ и $\mathbf{K}$ для узлов Дирихле

    **ШАГ 3f: Решить $\mathbf{K} \psi^{k+1} = \mathbf{F}^{k+1}$**

    **ШАГ 3g: Проверить сходимость**
    - $\text{error} = \|\psi^{k+1} - \psi^k\| / \|\psi^{k+1}\|$
    - Если $\text{error} < \varepsilon$: **ВЫХОД**
    - Иначе: $k = k+1$, вернуться в начало цикла

    **КОНЕЦ**

    Вывести $\psi$, $\omega$, $S$. Построить линии тока, границу $\psi=0$.
    """)